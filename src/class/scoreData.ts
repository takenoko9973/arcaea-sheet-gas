export class ScoreData {
    sumScore: number;
    lostScore: number;

    constructor(sumScore: number, lostScore: number) {
        this.sumScore = sumScore;
        this.lostScore = lostScore;
    }
}
